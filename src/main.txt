#include <Arduino.h>
// #include <Wire.h>

#define ls0 2
#define ls1 3
#define ls2 4
#define ls3 5
#define pinProx 6

#define IN1 8
#define IN2 9
#define IN3 10
#define IN4 11
#define ina 2
#define inb 3
#define inc 4
#define ind 5
#define ine 6
#define inf 7
#define ing 17

void setup()
{
  Serial.begin(115200);
  pinMode(ls0, INPUT);
  pinMode(ls1, INPUT);
  pinMode(ls2, INPUT);
  pinMode(ls3, INPUT);

  pinMode(IN1, OUTPUT);
  pinMode(IN2, OUTPUT);
  pinMode(IN3, OUTPUT);
  pinMode(IN4, OUTPUT);
  pinMode(ina, OUTPUT);
  digitalWrite(IN1, LOW);
  digitalWrite(IN2, LOW);
  digitalWrite(IN3, LOW);
  digitalWrite(IN4, LOW);
  digitalWrite(ina, LOW);
  digitalWrite(inb, LOW);
  digitalWrite(inc, LOW);
  digitalWrite(ind, LOW);
  digitalWrite(ine, LOW);
  digitalWrite(inf, LOW);
  digitalWrite(ing, LOW);

  // Wire.begin(19);
  // Wire.onReceive(receiveEvent);
}

void loop()
{
  if (digitalRead(ls0))
  {
    Serial.println("ls 1 terdeteksi");
    // digitalWrite(IN1, LOW);
    // digitalWrite(IN2, HIGH);
    // digitalWrite(IN3, HIGH);
    // digitalWrite(IN4, LOW);
  }
  if (digitalRead(ls1))
  {
    Serial.println("ls 2 terdeteksi");
    // digitalWrite(IN1, HIGH);
    // digitalWrite(IN2, LOW);
    // digitalWrite(IN3, HIGH);
    // digitalWrite(IN4, LOW);
  }
  if (digitalRead(ls2))
  {
    Serial.println("ls 3 terdeteksi");
    // digitalWrite(IN1, LOW);
    // digitalWrite(IN2, LOW);
    // digitalWrite(IN3, LOW);
    // digitalWrite(IN4, LOW);
  }
  if (digitalRead(ls3))
  {
    Serial.println("ls 4 terdeteksi");
    delay(500);
  }
}